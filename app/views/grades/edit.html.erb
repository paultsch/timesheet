<h1 class="text-center">Edit a Grade</h1>

<%= form_for @grade do |f| %>
  <div class="field">
    <%= f.label :grade_level %>
    <%= f.number_field :grade_level %>
  </div>

  <div class="actions">
    <%= f.submit class:'btn btn-primary'%>
  </div>

<% end %>
